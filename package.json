{
  "dependencies": {
    "bootstrap": "^5.3.2"
  },
  "scripts": {
    "compile:sass": "node-sass src/scss -o dist/css",
    "watch:sass": "npm run compile:sass -- --watch"
  },
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
